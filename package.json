{
  "name": "motion-handoff-timeline-builder",
  "version": "1.0.0",
  "description": "Figma plugin para gerar motion handoff timelines",
  "scripts": {
    "build": "tsc",
    "watch": "tsc --watch"
  },
  "devDependencies": {
    "@figma/plugin-typings": "^1.98.0",
    "typescript": "^5.3.0"
  }
}
